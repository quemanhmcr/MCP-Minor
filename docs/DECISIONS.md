# Decision Log

Append-only. Each entry states the decision, context, consequence, and revisit trigger. Do not edit past entries; add a superseding entry instead. Gate-required decisions must be recorded here before implementation begins.

---

| Date | Decision | Context | Consequence | Revisit trigger |
| --- | --- | --- | --- | --- |
| 2026-04-29 | Build standalone MCP server; do not boot Dirac's `ToolExecutor` | Upstream handlers depend on VS Code/UI/telemetry/task services | All tools re-implemented as pure behavior; no host coupling | Dirac publishes a standalone runtime worth adopting |
| 2026-04-29 | Transport: stdio only | No real HTTP/SSE consumer exists | No HTTP/SSE server code; client must drive via stdio | HTTP/SSE consumer emerges |
| 2026-04-29 | `dirac/` is a pinned read-only submodule at `e827ec30d4cdae078588df2040f203b780d657ad` | Preserves upstream reference integrity | No edits to `dirac/`; submodule pin changes need a Decision entry | Upstream publish a relevant fix or breaking change |
| 2026-04-29 | TypeScript NodeNext ESM with explicit `.js` imports | Matches runtime package expectations; avoids ambiguous module resolution | All imports use `.js` extension; no CJS compat shims | Node or TypeScript changes module resolution defaults |
| 2026-04-29 | Runtime context is slim: cwd, session id, workspace roots | Isolates MCP tools from Dirac host state | No global task/provider/UI state in handlers | Long-lived session state becomes a product requirement |
| 2026-04-29 | Workspace-root containment + symlink rejection for file tools | Direct filesystem tools need explicit safety boundaries without a host approval UI | All file reads and edits reject paths outside configured roots and reject symlink escapes | Consumer requires controlled cross-root access |
| 2026-04-29 | Deterministic session-scoped anchors (A + 6 base-36 chars) instead of Dirac random dictionary words | Reproducible MCP tests; stable sessions; lower token cost than dictionary words | Anchors differ from Dirac upstream; behavior invariants preserved | Session length or uniqueness requirements change |
| 2026-04-29 | `edit_file` conservative by default: require exact oldText, non-overlap, same-session anchors, full-file hash freshness, no partial writes | Direct filesystem mutation has no host approval UI; blast radius must be minimal | Edit rejects any ambiguity; agents must re-read on staleness | Consumer demonstrates that strict validation is a practical obstacle |
| 2026-04-29 | `edit_file` is single-file replace-only | Reduces blast radius while preserving core anchored edit behavior | No multi-file batching; no insert/end-anchor; see Architecture Gates for path to multi-file | Multi-file editing gate satisfied |
| 2026-04-29 | Defer `.diracignore` | Workspace containment plus built-in skips are sufficient for v1 tools | No ignore file parsing; consumers cannot add project-specific excludes | Consumer ignore-rule pain reported (see Architecture Gates) |
| 2026-04-29 | System `rg` for `search_files` | Avoids bundling/licensing decisions until necessary | Deployment requires system ripgrep; explicit error if absent | Consumer needs bundled rg; packaging/licensing reviewed |
| 2026-04-29 | Pin `web-tree-sitter@0.22.6` + `tree-sitter-wasms@^0.1.13` | Newer `web-tree-sitter@0.26` failed to load selected grammar binaries | Grammar loading is pinned; upgrading requires re-testing all grammar binaries | Grammar or runtime compatibility issue found at pin |
| 2026-04-29 | Copy tree-sitter runtime, grammar WASM, and query assets into `dist` | Built output must not depend on source-tree assets at runtime | Build step copies assets; built-output smoke validates resolution | Asset packaging strategy changes |
| 2026-04-29 | Start AST tooling with `get_file_skeleton` before `get_function` | Validates parser/query/output integration before narrower function lookup | Skeleton is the AST foundation; function lookup builds on it | Skeleton is replaced or deprecated |
| 2026-04-30 | Compact task-oriented `view` defaults; `view:"full"` for rich JSON; `format` aliases kept | Tool output should reduce model context, not exceed raw file reads | All tools default to compact; agents opt in to full explicitly | Output size benchmarks change significantly |
| 2026-04-30 | `view` replaces `format` as primary API; `format` aliases maintained | `view` separates task intent from serialization detail; less ambiguous than `format:"json"` | `view` is the canonical parameter; `format` aliases accepted in handlers | Consumer requires `format`-only API |
| 2026-04-30 | `read_file view:"edit"` required before `edit_file` | Default reads should be cheap; edit anchors paid only when mutation is intended | Agents must explicitly acquire edit anchors; default reads are cheaper | Anchor acquisition model changes |
| 2026-04-30 | Measure MCP-visible payload as `content` chars + `JSON.stringify(structuredContent).length` | Many clients expose both channels to models; both contribute to token cost | Benchmarks use this metric; output budgets account for structured channel | MCP spec changes structured content handling |
| 2026-04-30 | Documentation split: README = contracts, TRACKER = live state, NOTES = upstream research | Prevents drift; reduces session resume cost | Each doc has a single authoritative role; cross-linking for navigation | Doc count changes significantly |
| 2026-04-30 | `get_function` no-match and ambiguous suffix matches are structured results, not tool errors | Agents recover better from predictable result shapes; operational failures remain errors | No-match returns candidates; ambiguous returns options; agents can retry without user intervention | Consumer prefers error on ambiguity |
| 2026-04-30 | `function_names` is canonical; `functionNames` accepted as handler alias only | Avoids two visible fields with overlapping semantics | Schema shows `function_names`; handlers normalize `functionNames` | MCP schema convention changes |
| 2026-04-30 | `get_function` hashes: `bodyHash` for body identity, `viewHash` for context identity; full structured output omits source | Body identity must not drift with context; MCP clients often expose both channels | Two separate hash fields; agents use `bodyHash` for change detection | Hash semantics change |
| 2026-04-30 | `get_function` caps: path count, name count, returned result items (not raw paths × names) | Bounds parse/output cost without rejecting broad but small-result requests | Caps are per-dimension; not a cartesian product limit | Cap values need tuning from dogfood |
| 2026-05-01 | Restructure tracker from flat checklist to staged maturity roadmap with architecture gates | v1 kernel is complete; advancing without gates risks building symbol/multi-file/execution tools on unvalidated foundation | TRACKER.md is a cockpit dashboard; supporting docs hold detailed reasoning | Roadmap structure no longer fits the project's maturity |
